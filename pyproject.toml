[project]
name = "takt"
version = "0.1.2"
description = "Generyczny mechanizm kaskadowego sterowania cybernetycznego (Hierarchical Orchestrator) nad dowolnym drzewem stanu. Lokalne fale: Wave. Opcjonalna redukcja entropii: splot. Zero wiedzy domenowej."
authors = [
  { name = "mikolaj92", email = "mikolaj92@users.noreply.github.com" }
]
requires-python = ">=3.13"
license = "MIT"
dependencies = [
  "pydantic>=2.13.4",
  "typing-extensions>=4",
]

[project.optional-dependencies]
splot = ["splot-runtime"]
dev = [
  "pytest>=9.1.1",
  "pytest-cov>=5",
  "mypy>=1.10",
  "ruff>=0.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/takt"]

[tool.uv.sources]
splot-runtime = { git = "https://github.com/mikolaj92/splot.git" }

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-q --strict-markers"

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.mypy]
python_version = "3.13"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[project.urls]
Homepage = "https://github.com/mikolaj92/takt"
Repository = "https://github.com/mikolaj92/takt"
Issues = "https://github.com/mikolaj92/takt/issues"
